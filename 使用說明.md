# 屏東好好玩 — 使用說明

## 檔案說明
- `index.html` — 網頁主檔（設計、搜尋、篩選都在這裡）
- `data.js` — 所有票券資料（198 筆），網頁會自動讀取這個檔案
- `convert_excel_to_data.py` — 之後要「用 Excel 大量新增/更新」資料時，用這支程式重新產生 `data.js`
- `images/` — 頁面上用到的插畫／icon：`logo.png`（屏東觀光圈標誌，放在頁首標題前）、`badge-1936.png`（頁首右上角徽章）、`lighthouse.png`（鵝鑾鼻，「全部」分類 icon）、`victory-village.png`（勝利星村，「住宿」分類 icon）、`cruise.png`（郵輪，「交通票券」分類 icon）、`turtle.png`（海龜，「門票・體驗」分類 icon）
- `manifest.json` — PWA 設定檔，讓網頁可以「加入主畫面」
- `sw.js` — Service Worker，讓網頁加入主畫面後可以離線開啟、開啟速度更快
- `icons/` — App 圖示（加入主畫面後顯示在手機桌面的圖示）

檔案跟資料夾要放在**同一層**，`index.html` 才讀得到 `data.js` 和 `images/` 裡的圖片。上傳到網站空間時，記得連同 `images`、`icons` 整個資料夾一起上傳。

## 加入手機主畫面（PWA）
這個網頁已經加上 PWA 設定，使用者可以把它「加到手機主畫面」，開啟後會像 App 一樣全螢幕顯示、沒有網址列，桌面上也會有專屬圖示。

**重要前提：網站空間必須是 https 網址**（GitHub Pages、Netlify 預設都是 https，符合需求）。如果是 http 網址，「加入主畫面」的圖示/全螢幕效果仍可運作，但離線快取（Service Worker）的部分不會生效，資料還是需要連網才讀得到。

使用者操作方式：
- **iPhone（Safari）**：開啟網頁 → 點下方分享圖示 → 「加入主畫面」
- **Android（Chrome）**：開啟網頁 → 右上角選單 → 「加到主畫面」或「安裝應用程式」（有些手機會自動跳出安裝提示）

加入主畫面後，桌面圖示、名稱都會是「屏東好好玩」。之後 `data.js` 更新（新增票券），使用者不需要重新安裝，網頁會自動抓取最新資料（有做 network-first 快取策略，連網時一定會看到最新版本，離線時才會用之前瀏覽過的快取版本）。

## 怎麼上線讓 QR code 掃得到
把整個資料夾（至少 `index.html` + `data.js`）上傳到任何一個網頁空間即可，例如：
- 屏東縣政府/縣府觀光處自己的網站空間
- GitHub Pages（免費）
- Netlify / Vercel（免費，拖曳資料夾就能上線）
- Google Sites 或既有的活動網站掛一個子頁面

上線後拿到一個網址（例如 `https://xxxx.tw/pingtung-pass/`），再用這個網址產生 QR code 貼在文宣、立牌上即可。

## 之後怎麼新增資料（兩種方式，選一種就好）

### 方式一：改 Excel，重新整批轉換（推薦，量大或不熟程式碼時用）
1. 打開原本的 Excel 名單，比照原本的欄位（序號／商店名稱／地址／區域／電話／類別／連結）繼續往下新增資料列
   - 類別請填「住」「運」「遊」三個字其中一個
2. 電腦要有安裝 Python 及 pandas、openpyxl（跟我一開始處理資料的環境一樣）
3. 在終端機執行：
   ```
   python3 convert_excel_to_data.py 你的Excel檔名.xlsx
   ```
4. 執行完會自動產生新的 `data.js`，把它蓋掉舊檔、重新上傳到網站空間即可，`index.html` 完全不用動

### 方式二：直接把更新後的 Excel 傳給我
如果不想自己跑程式，之後有新增的店家/票券，直接把整理好的 Excel（或新增的那幾筆）貼給我，我幫您重新產生 `data.js`，您只要把新檔案換上網站空間即可，畫面設計都不會受影響。

## 目前資料分類說明
原始 Excel 的「類別」欄位只有「住／運／遊」三種代碼，目前對應為：
- 住 → 住宿（183 筆）
- 運 → 交通票券（3 筆）
- 遊 → 門票・體驗（12 筆，門票類跟體驗類目前合併顯示）

如果之後想把「門票」和「體驗」拆成兩個獨立分類，只要在 Excel 裡把「遊」改成更細的代碼（例如「票」「驗」），並同步告訴我或修改 `convert_excel_to_data.py` 裡的 `CAT_MAP` 對照表即可。

## 其他小提醒
- 有些店家在原始資料中有 2 個訂購連結（例如同時上架 Klook 跟 KKday），網頁會自動顯示 2 個訂購按鈕
- 電話號碼點下去會直接撥號（適合用手機瀏覽時使用）
- 搜尋框可以搜尋店家名稱、地址、地區關鍵字

## 點擊統計（Google Analytics）
網頁已經埋好追蹤程式碼，只要完成以下設定，就能在 Google Analytics 後台看到「每一家店、每個平台被點了幾次前往訂購」。設定前資料不會顯示，也不會影響網頁正常使用。

**設定步驟：**
1. 前往 [analytics.google.com](https://analytics.google.com)，用 Google 帳號登入（沒有帳號的話免費申請一個即可）
2. 建立一個新的「資源」（Property），名稱可以取「屏東好好玩」，接著建立一個「網路」資料串流，填入您網站的正式網址
3. 建立完成後，畫面會給您一組像 `G-XXXXXXXXXX` 的「Measurement ID」（衡量 ID），把它複製起來
4. 打開 `index.html`，搜尋 `G-XXXXXXXXXX`（總共會出現 2 個地方），全部換成您自己的 ID，存檔後重新上傳覆蓋
5. 到您的網站點幾次「前往訂購」按鈕測試，回到 GA 後台左側選單「報表」→「即時」，應該就能看到剛剛的點擊事件

**之後怎麼看統計：**
- GA 後台左側「報表」→「參與」→「事件」，可以看到 `book_click` 這個事件的總點擊次數
- 想細看「哪一家店」「哪個平台」被點最多，需要先在 GA 後台「管理」→「自訂定義」，把以下 4 個參數註冊成「自訂維度」，才能在報表中依照它們篩選／分組：
  - `listing_name`（店家名稱）
  - `platform`（OTA 平台，如 Klook、KKday）
  - `category`（分類：住宿／交通票券／門票・體驗）
  - `region`（地區，如恆春、小琉球）
- 註冊好自訂維度後，用「探索」（Explore）報表，把 `book_click` 事件搭配這些維度做成表格，就能清楚看到每家店、每個平台的點擊排行

這個設定只需要做一次，之後不管新增多少票券資料，都會自動一起被追蹤，不用額外處理。

## 新功能說明

### 分享按鈕
每張卡片右上角有一個分享圖示。手機上會直接跳出系統分享選單（可以分享到 LINE、簡訊等）；電腦瀏覽器沒有分享選單的話，會自動把連結複製到剪貼簿，貼給朋友的人打開後會自動幫忙搜尋到那家店。

### 我的收藏
每張卡片右上角的愛心圖示可以收藏／取消收藏，資料存在使用者自己手機的瀏覽器裡（不會上傳、也看不到其他人收藏了什麼）。篩選列有一個「♥ 我的收藏」按鈕，點下去只會顯示收藏過的店家。

### 附近優先
篩選列的「📍 附近優先」按鈕，點下去會請求手機定位權限，再依照**店家所在鄉鎮的中心點**估算距離排序（不是店家精確座標，因為原始資料只有地址文字，沒有經緯度）。再點一次可以取消排序。

### 精選標記
卡片左上角會出現「⭐ 精選」標籤，精選店家也會自動排在同分類的最前面（開啟「附近優先」時則以距離為主）。有兩種方式可以標記：
1. **Excel／CSV 加一欄「精選」**：填「是」「V」或「1」，重新用 `convert_excel_to_data.py` 轉換即可
2. **直接改 `index.html`**：搜尋 `FEATURED_OVERRIDES`，把店名加進那個陣列裡（要跟 `data.js` 裡的名稱完全一致），存檔就生效，不用重新轉換資料。適合您看了 GA 報表、想手動標記「熱門」店家時使用

### 依 QR code 地點客製首頁
在網址後面加參數，就能讓不同地點的 QR code 掃出來預設顯示不同內容，例如：
- 貼在小琉球的 QR code：`https://您的網址/?region=小琉球`
- 貼在恆春、只想推門票體驗的 QR code：`https://您的網址/?region=恆春&cat=門票・體驗`
- 想讓 QR code 直接搜尋到指定店家：`https://您的網址/?q=店家名稱`

網址列的中文字大部分 QR code 產生器會自動處理，如果遇到亂碼問題，把中文部分改成網址編碼（例如「小琉球」→ `%E5%B0%8F%E7%90%89%E7%90%83`）即可，或是先在瀏覽器打開一次網址，直接複製網址列上瀏覽器自動編碼好的版本來產生 QR code。
